{{ "{% form_theme edit_form 'ToaTwitterBootstrapBundle:Form:widgets.html.twig' %}" }}

{{ "{% extends '" ~ toa_twitter_bootstrap.template ~ "' %}" }}
{{ "{% block " ~ toa_twitter_bootstrap.block ~ " %}" }}

<h1 class="page-header">{{ "{{ '" ~ entity ~ "'|trans }} {{ 'edit'|trans }}" }}</h1>

<form class="form-horizontal" action="{{ "{{ path('"~ route_name_prefix ~"_update', { 'id': entity.id }) }}" }}" method="post" {{ "{{ form_enctype(edit_form) }}" }}>
    {{ "{{ form_widget(edit_form) }}" }}
    <p>
        <button class="btn" type="submit">
            <i class="icon-pencil"></i>
            {{ "{{ 'edit'|trans }}" }}
        </button>
    </p>
</form>

{% set hide_edit, hide_delete = true, false %}
{% include 'views/others/record_actions.html.twig' %}

{{ "{% endblock %}" }}